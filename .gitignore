# Node / web
**/node_modules/
**/.package-lock.json
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Builds / caches (adjust if you use these)
# NOTE: the repo-root dist/ holds GENERATED ontology release artifacts that are
# intentionally tracked (downstream consumers + CI read them). Ignore nested
# build dirs (e.g. webapp/dist) but not the ontology dist/.
**/webapp/dist/
build/
.cache/
.next/
coverage/

# Python virtualenv
.venv/

# WIDOCO documentation: generated HTML site + cached tool jar (published via CI)
docs/widoco/
tooling/.widoco/
